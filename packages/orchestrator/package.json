{
  "name": "@modes/orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "modes orchestration package - fan-out to CLI agents in isolated git worktrees, cross-review, JSONL instrumentation (Orca protocol port, see docs/port-spec.md)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "vitest": "^4.1.0"
  }
}
